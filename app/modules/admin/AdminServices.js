'use strict';

angular.module('myApp.admin')
  .factory('AdminServices', ['$http', '$q', '$timeout', 'SettingServices',
      function ($http, $q, $timeout, SettingServices) {
          var obj = {};

          return obj;
      }
	])
;
